# Per-deployment output (contains environment-specific item IDs)
deploy_output.json

# Generated at deploy time from sources (contain environment-specific IDs).
# Regenerated by deploy/deploy.py and deploy/report_builder.py on every run.
notebooks/01_load_data.ipynb
report/definition.pbir

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local secrets / tokens (never commit)
.env
*.token
